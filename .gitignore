# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Python bytecode generated by the AI service validation/runtime
__pycache__/
*.py[cod]

node_modules
dist
dist-ssr
*.local

# Secrets — VITE_SUPABASE_* live here; must stay out of git, present on disk/VPS
.env
.env.local
.env.*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# AIVOUX runtime
.aivoux/handoffs/
.aivoux/telemetry/
.aivoux/.session-tier
.aivoux/session-digest.md
.aivoux/gates/
# Handoff de sessao e estado LOCAL: pode conter detalhe operacional de infra
# (hosts, paths na VPS, vulnerabilidade ainda aberta). Repo e publico.
.aivoux/handoff-*.md
# Documentacao de incidente fica LOCAL enquanto a vulnerabilidade estiver aberta:
# em repo publico ela vira mapa de alvo. Publicar quando o status virar resolvido.
docs/security/
